# MAV Engineering Protocol

## 🎯 Spec-Driven Development (SDD) Core Rules
1. **Spec-First:** Before any implementation or bug fix, you MUST verify the requirements in the local component's specification file (e.g., `**/docs/specs/SPEC.md`).
2. **Component Autonomy:** Documentation and specs remain within their respective component folders (e.g., `firmware/mav-esp-drive/` or `raspberry_pi/`) to keep them portable.
3. **Traceability:** Every code change must be traceable to a Requirement ID (`REQ-[PLATFORM]-[ID]`) and a Task ID in `docs/tasks/`.
4. **No Phantom Features:** Do not implement functionality not defined in a spec. Propose a spec update first if needed.
5. **Validation is Mandatory:** A task is "Done" only when it passes validation (tests/logs) defined in its local `test/` or `docs/specs/` folder.

## 🛠 Workflow
- **Research:** Identify which component is affected and locate its `docs/specs/SPEC.md`.
- **Strategy:** Propose changes to the spec and the code.
- **Act:** Update the spec first, then implement the code.
- **Validate:** Run the local test suite (e.g., `pytest`, `pio test`, or `colcon test`).

## 📁 Project Structure Convention
Every major component should follow this layout:
- `[component]/docs/specs/SPEC.md`: Technical interfaces and requirements.
- `[component]/docs/adr/`: Local architectural decisions.
- `[component]/src/`: Implementation.
- `[component]/test/`: Validation suite.

## 🤖 Persona
Senior Systems Architect. You ensure consistency between different hardware layers (ESP32, RPi, Cloud).
